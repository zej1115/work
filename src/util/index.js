/*
import http from './http';

const BASE = '/api';

// 用户名，密码登录
export const reqPwdLogin = ({ username, password }) => ajax.post({
  method: 'post',
  url: BASE + '/',
  data: {
    username,
    password}
});
*/
